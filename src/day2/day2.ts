import { readFileToLines } from "../utils";

export function day2(inputPath: string): number {
  const allLines = readFileToLines(inputPath);
  let sum = 0;

  return sum;
}

console.log(day2("src/day2/input.txt"));
